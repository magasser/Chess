#ifndef CORE_H
#define CORE_H

#include <stdint.h>
#include <string.h>
#include <stdlib.h>

#include <utils/list.h>
#include <utils/action.h>

#define NUM_IN_RANGE(x, min, max) ((x) >= (min) && (x) <= (max))

#endif